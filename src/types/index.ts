
import { ITable } from '../models/Table';
import { IRestaurant } from '../models/Restaurant';
import { IReservation } from '../models/Reservation';

// Extend Express Request interface if needed (not strictly necessary here)
